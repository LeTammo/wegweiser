# Backend configuration
PORT=3001
HOST=127.0.0.1

# Frontend configuration (if served separately)
# For Vite, this needs to be in frontend/.env or set during build
VITE_API_URL=http://localhost:3001/api
